{
  "name": "@archivist/backend",
  "private": true,
  "scripts": {
    "build": "cargo build",
    "check": "cargo check",
    "fmt": "cargo fmt --all -- --check",
    "lint": "cargo clippy --all-targets --all-features -- -D warnings",
    "test": "cargo test",
    "typecheck": "cargo check",
    "dev": "echo backend-dev-disabled",
    "dev:vercel": "vercel dev --yes --listen 3100"
  }
}
