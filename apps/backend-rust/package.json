{
  "name": "@archivist/backend-rust",
  "private": true,
  "scripts": {
    "build": "cargo build",
    "check": "cargo check",
    "fmt": "cargo fmt --all -- --check",
    "lint": "cargo clippy --all-targets --all-features -- -D warnings",
    "test": "cargo test",
    "typecheck": "cargo check",
    "dev": "vercel dev"
  }
}
